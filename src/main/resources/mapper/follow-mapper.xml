<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		
<mapper namespace="com.depromeet.hay.mapper.FollowMapper">

    <insert id="add" parameterType="com.depromeet.hay.domain.Follow">
        INSERT INTO follow VALUES (#{followingId}, #{followerId});
    </insert>
</mapper>